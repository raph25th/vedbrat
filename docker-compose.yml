services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cfa
      POSTGRES_PASSWORD: cfa
      POSTGRES_DB: cfa_crm
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
